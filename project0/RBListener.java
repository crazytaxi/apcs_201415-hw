import java.awt.event.*;
import java.awt.*;

public class RBListener implements ActionListener{
	private CColor bref;

	public RBListener (CColor b) {
		this.bref = b;
	}

	@Override
	public void actionPerformed(ActionEvent e) {
		//Color tmp = new Color((float)Math.random(), (float)Math.random(), (float)Math.random(), (float)Math.random());
		//this.bref.setBackground(tmp);
		this.bref = 
	}
}
